@{
    ViewBag.Title = "Welcome to EasyTransfer";
}

<div class="container mt-5 text-center">
    <h1 class="mb-4">Welcome to EasyTransfer</h1>
    <p class="lead mb-5">Log in or Register a new account:</p>

    <div class="row justify-content-center">
        <div class="col-md-4 mb-3">
            <a href="@Url.Action("Register", "Customer")" class="btn btn-lg btn-primary btn-block">
                Register
            </a>
        </div>
        <div class="col-md-4 mb-3">
            <a href="@Url.Action("Login", "Customer")" class="btn btn-lg btn-success btn-block">
                Login
            </a>
        </div>
    </div>
</div>
